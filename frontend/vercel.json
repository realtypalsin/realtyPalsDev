{
  "rewrites": [
    {
      "source": "/api/:path*",
      "destination": "https://propfyndrdev.onrender.com/api/:path*"
    }
  ],
  "buildCommand": "npm run db:generate && npm run build",
  "installCommand": "npm install --legacy-peer-deps"
}
