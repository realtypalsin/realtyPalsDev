# Worktrees
.worktrees/

# Dependencies
node_modules/
/.pnp
.pnp.js

# Build & Output
.next/
/out/
/dist/
/build/
vercel_out.html
*.tsbuildinfo

# Vercel (created by `vercel link`)
.vercel/

# Environment — never commit secrets
.env
.env*
!.env.example
frontend/.env*
!frontend/.env.example
backend/.env*
!backend/.env.example

# Database & Storage
prisma/*.db
prisma/*.db-journal
ruvector.db
.tokensave/*.db*

# Testing & Logs
/coverage/
frontend/test-results/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
backend-err.txt
backend-out.txt
dev.log

# Temporary / Scratch / Internal Scripts
git_diff.txt
image.png
backend/scratch/
backend/test-gemini.mjs
launch-your-agent/

# Misc
.DS_Store
*.pem
*.bak

# Added by code-review-graph
.code-review-graph/

# Agents and workspace configs
.claude/
.agents/
.mcp.json
.impeccable/
.superpowers/

# Local audits / notes
audit*.md
# Additional ignores for any nested build/artifact directories
**/.next/
**/.vercel/
# Local env files that may contain secrets
**/.env.local
**/.env.development
**/.env.production
**/.env.test

# Local tool caches / indexes (large, machine-specific, regenerable)
.tokensave/
.code-review-graph/
.impeccable/
.gemini/
.planning/
.taskmaster/
*.rvf
*.rvf.lock

# Playwright HTML report (build artifact)
frontend/playwright-report/
frontend/test-results/

# Local QA harness (see NEXT_DIST_DIR in frontend/next.config.js)
.next-qa/
.playwright-mcp/

# Corpus run artifacts — regenerated by scripts/corpus/run-corpus.ts on every
# run, large, and they churn on every measurement. The scripts and the input
# query files are versioned; the outputs are not.
backend/scripts/corpus/results-*.json
backend/dev-server.log

# Reference prompts dropped in for study. Third-party material, not product code.
SystemPrompts/

# Generated by scripts/audit-gaps-report.ts — regenerate, do not commit.
backend/gaps-*.csv
